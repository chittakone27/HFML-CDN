const ocaGetEvents = async (program, orgUnit, page, period, filters) => {
  return { hello: "hello" };
};

const ocaGetEventById = async (event) => {
  return { hello: "hello" };
};

const ocaSaveEvent = async (event) => {
  return { hello: "hello" };
};

const ocaDeleteEvent = async (event) => {
  return { hello: "hello" };
};
export { ocaGetEvents, ocaGetEventById, ocaSaveEvent, ocaDeleteEvent };
